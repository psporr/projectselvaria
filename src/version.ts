/**
 * 0.x.y — pre-1.0, so x/y carry the meaning major/minor normally would:
 *  - x bumps for new features or gameplay changes.
 *  - y bumps for fixes.
 * Bump this alongside package.json's "version" (kept in sync manually,
 * since this is a static string Vite can't read package.json into at
 * runtime without extra build config).
 */
export const GAME_VERSION = '0.10.1';
